# windows commands
# cli = command line interface
# windows dos prompt (cli to OS): Command + R => "cmd"
# linux cli = shells (bash, ...)
# OS = Operating System
# idle (cli to python): idle

# directory = folder
################## dos commands #############3
# cd = change directory
# dir = directory listing
# mkdir = make directory

# c:\_\c*\prog.100
# ie. c:\_\c4\prog.100
